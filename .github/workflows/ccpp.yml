name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: [windows-latest]
    
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: msbuild card.sln /p:Configuration=debug
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
